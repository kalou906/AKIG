# ============================================================================
# Configuration Docker - Une seule source de vérité pour toutes les variables
# ============================================================================
# Variables communes pour docker-compose.yml, backend, et frontend

# ============ PostgreSQL ============
DB_USER=akig_user
DB_PASSWORD=akig_secure_password_2025
DB_NAME=akig_db
DB_HOST=postgres
DB_PORT=5432

# ============ Backend API ============
NODE_ENV=production
APP_ENV=production
PORT=4000
DATABASE_URL=postgresql://akig_user:akig_secure_password_2025@postgres:5432/akig_db
JWT_SECRET=akig_jwt_secret_min_32_chars_change_in_prod_key_12345
CORS_ORIGIN=http://localhost:3000
LOG_LEVEL=info
FEATURE_FLAGS=payments,sms,dashboard,audit_logging,csv_import,pdf_export

# ============ Frontend ============
REACT_APP_API_URL=http://localhost:4000
REACT_APP_ENV=production
REACT_APP_VERSION=1.0.0

# ============ Monitoring ============
API_BASE_URL=http://localhost:4000/api
