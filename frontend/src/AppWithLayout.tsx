// AppWithLayout.tsx - Forces the router-based dashboard entry point
// We import the JSX version explicitly to bypass CRA's .tsx preference order

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore importing JSX keeps the original AKIG router and layout
import App from './App.jsx';

export default App;
